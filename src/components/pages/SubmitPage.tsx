// import { useFeedbackStore } from '../../stores/feedbackStore';

export default function SubmitPage() {
  // const create = useFeedbackStore((state) => state.create);

  return (
    <>
      <div tw="flex flex-col items-center p-10 bg-white rounded-xl"></div>
    </>
  );
}
